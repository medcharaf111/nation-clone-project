// This file contains the JavaScript code for the landing page.
// It may handle interactions, animations, or any dynamic content that needs to be displayed on the page.

document.addEventListener('DOMContentLoaded', function() {
    // Example of a simple interaction: show an alert when a button is clicked
    const button = document.getElementById('start-button');
    if (button) {
        button.addEventListener('click', function() {
            alert('مرحبا بك في تطبيقنا!');
        });
    }

    // Additional dynamic content or animations can be added here
});